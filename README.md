# Repository Workflows and Actions
## Workflows
### readme.yml
```yaml
            cat $file | grep 'inputs\|outputs' >> README.md
                cat $file | grep 'inputs\|outputs' >> README.md
```
## Composite Actions
